<%= simple_form_for([:user, @sessions]) do |f| %>

  <%= f.input :title, label:"Titre" %>
  <%= f.input :spot, label:"Spot" %>
  <%= f.input :date %>
  <%= f.input :description %>

  <%= f.button :submit, "Enregistre ta session", class: "btn-flat" %>

<% end %>
